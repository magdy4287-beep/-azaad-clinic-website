/* AZAAD CLINIC — Patient 360 Appointment Actions V4 */
(() => {
  'use strict';
  const $=id=>document.getElementById(id);
  const SUPABASE_URL='https://derofsthjivlkcdnojww.supabase.co';
  const CHECKIN_API=`${SUPABASE_URL}/functions/v1/azaad-frontdesk-checkin`;
  const KEY='sb_publishable_GC253fvQebNBsDOaKjWGRw_tPYJrgLa';
  const tr=(ar,en)=>(document.documentElement.lang||'ar').toLowerCase().startsWith('en')?en:ar;
  const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
  const token=()=>window.AZAAD?.state?.session?.access_token||sessionStorage.getItem('azaad_admin_token')||'';
  const toast=(m,error=false)=>window.showToast?window.showToast(m,error?'error':'success'):alert(m);
  async function rpc(fn,args){const t=token();if(!t)throw Error(tr('جلسة الإدارة غير موجودة أو منتهية.','The admin session is missing or expired.'));const r=await fetch(`${SUPABASE_URL}/rest/v1/rpc/${fn}`,{method:'POST',headers:{Authorization:`Bearer ${t}`,apikey:KEY,'Content-Type':'application/json',Accept:'application/json'},body:JSON.stringify(args)});const b=await r.json().catch(()=>null);if(!r.ok)throw Error(b?.message||b?.hint||b?.details||`HTTP ${r.status}`);return b;}
  async function checkIn(booking){const t=token();if(!t)throw Error(tr('جلسة الإدارة غير موجودة أو منتهية.','The admin session is missing or expired.'));const r=await fetch(CHECKIN_API,{method:'POST',headers:{Accept:'application/json',Authorization:`Bearer ${t}`,apikey:KEY,'Content-Type':'application/json'},body:JSON.stringify({booking_id:booking.id})});const b=await r.json().catch(()=>({}));if(!r.ok)throw Error(b?.error||b?.message||`HTTP ${r.status}`);return b;}
  function addStyle(){if($('patientAppointmentActionsStyle'))return;const s=document.createElement('style');s.id='patientAppointmentActionsStyle';s.textContent='.p360-actions{display:flex;gap:8px;flex-wrap:wrap;margin:12px 0;padding:12px;background:#f7f8fb;border:1px solid #e4e7ee;border-radius:12px}.p360-action-title{width:100%;font-weight:900;color:#17214f}.p360-action-status{font-size:12px;color:#5f6880;width:100%}.p360-actions button{min-height:42px}.p360-pay-box{display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;width:100%;margin-top:8px}.p360-pay-box input,.p360-pay-box select{padding:9px;border:1px solid #d9deea;border-radius:9px}@media(max-width:700px){.p360-pay-box{grid-template-columns:1fr}}';document.head.appendChild(s);}
  function appointmentFromRow(row){const code=row.textContent.match(/AZD-[A-Z0-9-]+/)?.[0]||'';const rows=Array.isArray(window.AZAAD_PATIENTS?.state?.bookings)?window.AZAAD_PATIENTS.state.bookings:[];return rows.find(x=>String(x.booking_code||'')===code)||null;}
  function doctorUrl(booking){const params=new URLSearchParams();if(booking?.id)params.set('booking_id',booking.id);if(booking?.patient_id)params.set('patient_id',booking.patient_id);return `doctor-dashboard.html?${params.toString()}`;}
  function renderActions(){const content=$('p360Content');if(!content)return;[...content.querySelectorAll('.p360-row')].filter(r=>/🔖/.test(r.textContent||'')&&/⏰/.test(r.textContent||'')).forEach(row=>{if(row.dataset.actionBound==='1')return;row.dataset.actionBound='1';const booking=appointmentFromRow(row);if(!booking)return;const bar=document.createElement('div');bar.className='p360-actions';bar.innerHTML=`<div class="p360-action-title">🏥 ${tr('إجراءات الموعد','Appointment Actions')}</div><div class="p360-action-status" data-action-state>🚦 ${esc(booking.status||'—')} · 💳 ${esc(booking.payment_status||'unpaid')}</div>`;const add=(label,cls,handler)=>{const b=document.createElement('button');b.type='button';b.className=`btn ${cls}`;b.textContent=label;b.onclick=handler;bar.appendChild(b);return b;};const status=String(booking.status||'').toLowerCase().replaceAll('-','_');const payment=String(booking.payment_status||'').toLowerCase();if(['pending','confirmed'].includes(status))add(`🟢 ${tr('دخول / Check-in','Check-in')}`,'btn-success',async()=>{if(!confirm(tr('تأكيد دخول المريض لهذا الموعد الآن؟','Confirm patient check-in now?')))return;try{const result=await checkIn(booking);const d=result?.data||result;bar.querySelector('[data-action-state]').textContent=`🟢 ${d?.status||'checked_in'} · 🧾 ${d?.invoice_number||tr('فاتورة مفتوحة','Open invoice')} · 💳 ${d?.invoice_status||'unpaid'}`;toast(d?.late_arrival?tr('🟡 تم الدخول متأخرًا وإنشاء/تأكيد الفاتورة.','🟡 Checked in late; invoice created/confirmed.'):tr('🟢 تم دخول المريض وإنشاء/تأكيد الفاتورة.','🟢 Patient checked in; invoice created/confirmed.'));window.AZAAD_PATIENTS?.load?.();}catch(e){toast(e.message,true);}});add(`🧾 ${tr('الفاتورة / الدفع','Invoice / Payment')}`,'btn-primary',()=>{[...content.parentElement.querySelectorAll('.p360-tab')].find(b=>b.dataset.view==='invoices')?.click();});if(booking.invoice_id||payment!=='paid'){const payBox=document.createElement('div');payBox.className='p360-pay-box';payBox.innerHTML=`<input type="number" min="0.01" step="0.01" placeholder="${tr('المبلغ','Amount')}" data-pay-amount><select data-pay-method><option value="cash">💵 ${tr('نقدي','Cash')}</option><option value="card">💳 ${tr('بطاقة','Card')}</option><option value="vodafone_cash">📱 Vodafone Cash</option><option value="wallet">📱 ${tr('محفظة','Wallet')}</option><option value="bank_transfer">🏦 ${tr('تحويل بنكي','Bank transfer')}</option><option value="other">🔹 ${tr('أخرى','Other')}</option></select><button type="button" class="btn btn-gold" data-pay-btn>💳 ${tr('تسجيل الدفع','Record Payment')}</button>`;bar.appendChild(payBox);payBox.querySelector('[data-pay-btn]').onclick=async()=>{const amount=Number(payBox.querySelector('[data-pay-amount]').value);const method=payBox.querySelector('[data-pay-method]').value;if(!booking.invoice_id){toast(tr('يجب تنفيذ Check-in أولًا لإنشاء الفاتورة.','Run Check-in first to create the invoice.'),true);return}if(!(amount>0)){toast(tr('أدخل مبلغ الدفع.','Enter a valid payment amount.'),true);return}if(!confirm(tr(`تسجيل دفعة بقيمة ${amount}؟`,`Record a payment of ${amount}?`)))return;try{const p=await rpc('clinic_record_payment',{p_invoice_id:booking.invoice_id,p_amount:amount,p_method:method,p_reference:null,p_notes:tr('دفع من Patient 360','Payment from Patient 360')});const verified=String(p?.verification_status||'').toLowerCase()==='verified';bar.querySelector('[data-action-state]').textContent=`${verified?'🟢':'🟡'} ${esc(booking.status||'checked_in')} · 💳 ${esc(p?.verification_status||'pending')}`;toast(method==='bank_transfer'?tr('🟡 تم تسجيل التحويل البنكي ويحتاج Verification قبل إتاحة الطبيب.','🟡 Bank transfer recorded and requires verification before Doctor access.'):tr('🟢 تم تسجيل الدفع وتحديث حالة الفاتورة.','🟢 Payment recorded and invoice status updated.'));window.AZAAD_PATIENTS?.load?.();}catch(e){toast(e.message,true)}};}add(`🩺 ${tr('تحويل للطبيب','Send to Doctor')}`,'btn-gold',()=>{const ready=['checked_in','checked_in_late','in_progress'].includes(String(booking.status||'').toLowerCase());const paid=String(booking.payment_status||'').toLowerCase()==='paid';if(!ready){toast(tr('🟡 يجب تنفيذ Check-in أولًا قبل تحويل المريض للطبيب.','🟡 Check-in must be completed before sending the patient to the Doctor Queue.'),true);return;}if(!paid){toast(tr('💳 يجب تسوية الفاتورة/تأكيد الدفع قبل دخول المريض للطبيب.','💳 The invoice must be settled/payment verified before Doctor access.'),true);return;}toast(tr('🩺 تم تجهيز المريض للطبيب. فتح Doctor Dashboard...','🩺 Patient is ready for the Doctor. Opening Doctor Dashboard...'));window.location.href=doctorUrl(booking);});row.appendChild(bar);});}
  function install(){addStyle();new MutationObserver(renderActions).observe(document.body,{childList:true,subtree:true});renderActions();}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(install,500),{once:true});else setTimeout(install,500);
})();

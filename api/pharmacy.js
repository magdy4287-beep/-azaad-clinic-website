import { neon } from '@neondatabase/serverless';
const COOKIE='azaad_admin_appwrite_session';
const READ_ROLES=new Set(['OWNER','ADMIN','MANAGER','SECRETARY','RECEPTION','DOCTOR','NURSE','CASHIER']);
const WRITE_ROLES=new Set(['OWNER','ADMIN','MANAGER','DOCTOR','NURSE']);
const DISPENSE_ROLES=new Set(['OWNER','ADMIN','MANAGER','DOCTOR','NURSE']);
function json(res,b,s=200){res.statusCode=s;res.setHeader('content-type','application/json; charset=utf-8');res.setHeader('cache-control','no-store');res.end(JSON.stringify(b));}
function getCookie(req){const m=String(req.headers.cookie||'').match(new RegExp(`(?:^|;\\s*)${COOKIE}=([^;]*)`));return m?decodeURIComponent(m[1]):'';}
async function account(secret){const e=String(process.env.APPWRITE_ENDPOINT||'').replace(/\/$/,'');const p=String(process.env.APPWRITE_PROJECT_ID||'').trim();if(!e||!p||!secret)return null;const c=`a_session_${p}=${secret}; a_session_${p}_legacy=${secret}`;const r=await fetch(`${e}/account`,{headers:{'X-Appwrite-Project':p,Cookie:c,accept:'application/json'}});return r.ok?r.json():null;}
async function auth(req){const u=await account(getCookie(req));if(!u?.$id)return null;const d=String(process.env.DATABASE_URL||'').trim();if(!d)throw new Error('DATABASE_RUNTIME_NOT_CONFIGURED');const sql=neon(d);const rows=await sql`select id,role,active,account_status from public.clinic_staff where auth_user_id=${u.$id} and active=true limit 1`;const staff=rows[0],role=String(staff?.role||'').toUpperCase();if(!staff||staff.account_status!=='active'||!READ_ROLES.has(role))return null;return{sql,staff,role};}
function id(v,n){const x=String(v||'').trim();if(!/^[0-9a-f-]{36}$/i.test(x))throw new Error(`${n}_required`);return x;}
async function body(req){if(req.body&&typeof req.body==='object')return req.body;return new Promise(r=>{let x='';req.on('data',c=>x+=c);req.on('end',()=>{try{r(x?JSON.parse(x):{})}catch{r({})}});req.on('error',()=>r({}))});}
async function event(sql,staff,patient,order,type,data={}){await sql`insert into public.clinic_pharmacy_events(actor_staff_id,patient_id,order_id,event_type,event_data) values(${staff},${patient||null},${order||null},${type},${JSON.stringify(data)}::jsonb)`;}
export default async function handler(req,res){try{const a=await auth(req);if(!a)return json(res,{error:'unauthorized'},401);const{sql,staff,role}=a;const u=new URL(req.url||'/','https://azaad.invalid');if(req.method==='GET'){const action=u.searchParams.get('action')||'dashboard';if(action==='dashboard'){const [locations,medications,orders,stock]=await Promise.all([sql`select * from public.clinic_pharmacy_locations where active=true order by pharmacy_type,code`,sql`select * from public.clinic_medications where active=true order by generic_name,brand_name limit 1000`,sql`select o.*,count(i.id)::int item_count from public.clinic_medication_orders o left join public.clinic_medication_order_items i on i.order_id=o.id where o.status not in ('CANCELLED','DISPENSED') group by o.id order by o.created_at desc limit 500`,sql`select s.*,l.code location_code,m.generic_name,m.brand_name from public.clinic_pharmacy_stock_lots s join public.clinic_pharmacy_locations l on l.id=s.location_id join public.clinic_medications m on m.id=s.medication_id where s.active=true order by s.expiry_date nulls last limit 1000`]);return json(res,{locations,medications,orders,stock,provider:'appwrite-neon'});}if(action==='patient'){const p=id(u.searchParams.get('patient_id'),'patient_id');const orders=await sql`select o.*,json_agg(json_build_object('id',i.id,'medication_id',i.medication_id,'dose',i.dose,'frequency',i.frequency,'route',i.route,'quantity_requested',i.quantity_requested,'quantity_dispensed',i.quantity_dispensed,'instructions',i.instructions)) as items from public.clinic_medication_orders o left join public.clinic_medication_order_items i on i.order_id=o.id where o.patient_id=${p} group by o.id order by o.created_at desc limit 100`;const dispenses=await sql`select d.*,l.code location_code,m.generic_name,m.brand_name,s.lot_number from public.clinic_pharmacy_dispenses d join public.clinic_pharmacy_locations l on l.id=d.location_id join public.clinic_medications m on m.id=(select medication_id from public.clinic_medication_order_items where id=d.order_item_id) left join public.clinic_pharmacy_stock_lots s on s.id=d.lot_id where d.patient_id=${p} order by d.dispensed_at desc limit 200`;return json(res,{orders,dispenses,provider:'appwrite-neon'});}return json(res,{error:'unsupported_action'},400);}if(req.method!=='POST'||!WRITE_ROLES.has(role))return json(res,{error:'forbidden'},403);const b=await body(req),action=String(b.action||'').trim();if(action==='create_location'){const r=await sql`insert into public.clinic_pharmacy_locations(code,name,pharmacy_type) values(${String(b.code||'').trim().toUpperCase()},${String(b.name||'').trim()},${String(b.pharmacy_type||'INTERNAL')}) returning *`;await event(sql,staff.id,null,null,'LOCATION_CREATED',{location_id:r[0].id});return json(res,{row:r[0],provider:'appwrite-neon'},201);}if(action==='create_medication'){const r=await sql`insert into public.clinic_medications(generic_name,brand_name,strength,dosage_form,route,controlled,formulary_status,external_sellable) values(${String(b.generic_name||'').trim()},${String(b.brand_name||'').trim()||null},${String(b.strength||'').trim()||null},${String(b.dosage_form||'').trim()||null},${String(b.route||'').trim()||null},${Boolean(b.controlled)},${String(b.formulary_status||'ACTIVE')},${b.external_sellable!==false}) returning *`;await event(sql,staff.id,null,null,'MEDICATION_CREATED',{medication_id:r[0].id});return json(res,{row:r[0],provider:'appwrite-neon'},201);}if(action==='receive_stock'){const location=id(b.location_id,'location_id'),med=id(b.medication_id,'medication_id'),q=Number(b.quantity);if(!(q>0))return json(res,{error:'quantity_required'},400);const r=await sql`insert into public.clinic_pharmacy_stock_lots(location_id,medication_id,lot_number,expiry_date,quantity,unit_cost,supplier) values(${location},${med},${String(b.lot_number||'').trim()},${b.expiry_date||null},${q},${b.unit_cost||null},${String(b.supplier||'').trim()||null}) on conflict(location_id,medication_id,lot_number) do update set quantity=clinic_pharmacy_stock_lots.quantity+excluded.quantity,expiry_date=coalesce(excluded.expiry_date,clinic_pharmacy_stock_lots.expiry_date) returning *`;await sql`insert into public.clinic_pharmacy_inventory_events(location_id,lot_id,medication_id,event_type,quantity,actor_staff_id,event_data) values(${location},${r[0].id},${med},'RECEIPT',${q},${staff.id},${JSON.stringify({supplier:b.supplier||null})}::jsonb)`;return json(res,{row:r[0],provider:'appwrite-neon'},201);}if(action==='create_order'){const patient=id(b.patient_id,'patient_id');const r=await sql`insert into public.clinic_medication_orders(patient_id,encounter_id,source,prescriber_staff_id,priority,notes) values(${patient},${b.encounter_id?id(b.encounter_id,'encounter_id'):null},${String(b.source||'OUTPATIENT')},${staff.id},${String(b.priority||'ROUTINE')},${String(b.notes||'')||null}) returning *`;for(const x of Array.isArray(b.items)?b.items:[]){await sql`insert into public.clinic_medication_order_items(order_id,medication_id,dose,frequency,route,duration,quantity_requested,instructions) values(${r[0].id},${id(x.medication_id,'medication_id')},${String(x.dose||'').trim()||null},${String(x.frequency||'').trim()||null},${String(x.route||'').trim()||null},${String(x.duration||'').trim()||null},${Number(x.quantity_requested||1)},${String(x.instructions||'').trim()||null})`;}await event(sql,staff.id,patient,r[0].id,'MEDICATION_ORDER_CREATED',{source:r[0].source});return json(res,{row:r[0],provider:'appwrite-neon'},201);}if(action==='dispense'){if(!DISPENSE_ROLES.has(role))return json(res,{error:'forbidden'},403);const item=id(b.order_item_id,'order_item_id'),location=id(b.location_id,'location_id'),q=Number(b.quantity);if(!(q>0))return json(res,{error:'quantity_required'},400);const rows=await sql`select i.*,o.patient_id,o.status order_status,m.generic_name from public.clinic_medication_order_items i join public.clinic_medication_orders o on o.id=i.order_id join public.clinic_medications m on m.id=i.medication_id where i.id=${item} limit 1`;if(!rows[0])return json(res,{error:'order_item_not_found'},404);const lots=await sql`select * from public.clinic_pharmacy_stock_lots where location_id=${location} and medication_id=${rows[0].medication_id} and active=true and quantity>=${q} and (expiry_date is null or expiry_date>=current_date) order by expiry_date nulls last,created_at limit 1`;if(!lots[0])return json(res,{error:'stock_unavailable'},409);const d=await sql`insert into public.clinic_pharmacy_dispenses(order_id,order_item_id,patient_id,location_id,lot_id,quantity,dispensed_by) values(${rows[0].order_id},${item},${rows[0].patient_id},${location},${lots[0].id},${q},${staff.id}) returning *`;await sql`update public.clinic_pharmacy_stock_lots set quantity=quantity-${q},updated_at=now() where id=${lots[0].id}`;await sql`insert into public.clinic_pharmacy_inventory_events(location_id,lot_id,medication_id,event_type,quantity,reference_id,actor_staff_id) values(${location},${lots[0].id},${rows[0].medication_id},'DISPENSE',-${q},${d[0].id},${staff.id})`;await sql`update public.clinic_medication_order_items set quantity_dispensed=quantity_dispensed+${q} where id=${item}`;await sql`update public.clinic_medication_orders set status=case when not exists(select 1 from public.clinic_medication_order_items where order_id=${rows[0].order_id} and quantity_dispensed<quantity_requested) then 'DISPENSED' else 'PARTIALLY_DISPENSED' end,updated_at=now() where id=${rows[0].order_id}`;await event(sql,staff.id,rows[0].patient_id,rows[0].order_id,'MEDICATION_DISPENSED',{dispense_id:d[0].id,quantity:q});return json(res,{row:d[0],provider:'appwrite-neon'},201);}if(action==='reconcile'){const patient=id(b.patient_id,'patient_id');const med=b.medication_id?id(b.medication_id,'medication_id'):null;const r=await sql`insert into public.clinic_medication_reconciliation(patient_id,encounter_id,medication_id,action,source,notes,reviewed_by,reviewed_at) values(${patient},${b.encounter_id?id(b.encounter_id,'encounter_id'):null},${med},${String(b.reconciliation_action||'UNKNOWN')},${String(b.source||'PATIENT')},${String(b.notes||'')||null},${staff.id},now()) returning *`;await event(sql,staff.id,patient,null,'MEDICATION_RECONCILIATION_RECORDED',{reconciliation_id:r[0].id});return json(res,{row:r[0],provider:'appwrite-neon'},201);}return json(res,{error:'unsupported_action'},400);}catch(e){console.error('pharmacy boundary failure',{name:e?.name,message:e?.message});return json(res,{error:/_required$/.test(String(e?.message||''))?e.message:'pharmacy_boundary_failure'},/_required$/.test(String(e?.message||''))?400:500);}}
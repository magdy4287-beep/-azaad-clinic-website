import { neon } from '@neondatabase/serverless';

const COOKIE = 'azaad_admin_appwrite_session';
const json = (body, status = 200) => new Response(JSON.stringify(body), { status, headers: {'content-type':'application/json; charset=utf-8','cache-control':'no-store'} });
function cookieValue(request) { const raw = request.headers.get('cookie') || ''; const match = raw.match(new RegExp(`(?:^|;\\s*)${COOKIE}=([^;]*)`)); return match ? decodeURIComponent(match[1]) : ''; }
async function appwriteAccount(secret) { const endpoint=String(process.env.APPWRITE_ENDPOINT||'').replace(/\/$/,''); const project=String(process.env.APPWRITE_PROJECT_ID||'').trim(); if(!endpoint||!project||!secret)return null; const r=await fetch(`${endpoint}/account`,{headers:{'X-Appwrite-Project':project,accept:'application/json',Cookie:`a_session_${project}=${secret}`}}); if(!r.ok)return null; return r.json(); }
async function authorize(request,sql){ const user=await appwriteAccount(cookieValue(request)); if(!user?.$id)return null; const schema=await sql`select exists(select 1 from information_schema.tables where table_schema='public' and table_name='clinic_staff') as staff_table`; if(!schema[0]?.staff_table)return {unprovisioned:true}; const rows=await sql`select id,auth_user_id,role,active from public.clinic_staff where auth_user_id=${user.$id} and active=true limit 1`; const staff=rows[0]; if(!staff)return null; const role=String(staff.role||'').toUpperCase(); if(!['OWNER','ADMIN','MANAGER','SECRETARY','RECEPTION','FRONTDESK'].includes(role))return null; return {user,staff,role}; }
export default async function handler(request){ if(request.method!=='POST')return json({error:'method_not_allowed'},405); const databaseUrl=String(process.env.DATABASE_URL||'').trim(); if(!databaseUrl)return json({error:'backend_not_configured'},503); try{ const sql=neon(databaseUrl); const identity=await authorize(request,sql); if(!identity)return json({error:'authentication_required'},401); if(identity.unprovisioned)return json({error:'clinical_identity_backend_not_provisioned'},503); const body=await request.json().catch(()=>null); const bookingId=String(body?.booking_id||'').trim(); if(!bookingId)return json({error:'booking_id_required'},400); const rows=await sql`select id,booking_code,patient_id,patient_name,appointment_date,appointment_time,status,checked_in_at,checked_in_by,checkin_notes from public.clinic_bookings where id=${bookingId}::uuid limit 1`; const booking=rows[0]; if(!booking)return json({error:'booking_not_found'},404); if(booking.checked_in_at)return json({error:'already_checked_in',data:booking},409); return json({error:'clinical_transaction_boundary_not_provisioned'},503); }catch(error){ console.error('frontdesk-checkin boundary failure',{name:error?.name,message:error?.message}); return json({error:'clinical_backend_unavailable'},503); } }

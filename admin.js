import {createClient} from 'https://esm.sh/@supabase/supabase-js@2';const sb=createClient('https://derofsthjivlkcdnojww.supabase.co','');const $=x=>document.getElementById(x);$('loginBtn').onclick=async()=>{const {error}=await sb.auth.signInWithPassword({email:$('email').value,password:$('password').value});if(error){$('loginMsg').textContent=error.message;$('loginMsg').className='error';return}show()};async function show(){ $('login').hidden=true;$('dash').hidden=false;const {data,error}=await sb.from('clinic_bookings').select('booking_code,patient_name,patient_phone,appointment_date,appointment_time,status,mode').order('appointment_date',{ascending:false}).limit(100);if(error){$('bookings').textContent='تعذر تحميل الحجوزات.';return}$('bookings').innerHTML='<table class="table"><tr><th>رقم</th><th>المريض</th><th>الهاتف</th><th>التاريخ</th><th>الوقت</th><th>الحالة</th></tr>'+(data||[]).map(x=>`<tr><td>${x.booking_code||''}</td><td>${x.patient_name||''}</td><td>${x.patient_phone||''}</td><td>${x.appointment_date||''}</td><td>${String(x.appointment_time||'').slice(0,5)}</td><td><span class="badge">${x.status||''}</span></td></tr>`).join('')+'</table>'};sb.auth.getSession().then(({data})=>{if(data.session)show()});
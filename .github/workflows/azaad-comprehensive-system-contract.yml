name: AZAAD Comprehensive System Contract

on:
  push:
    branches: [main, 'codex/**']
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  comprehensive-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Verify workflow ownership
        run: python qa/workflow-ownership-gate.py

      - name: Run comprehensive system contract
        run: python qa/comprehensive-system-contract.py

      - name: Run platform kernel contract
        run: python qa/azaad-platform-kernel-contract.py

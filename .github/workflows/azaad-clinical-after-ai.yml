name: Azaad Clinical Authorization After AI Gate

on:
  workflow_run:
    workflows: ["Azaad AI Operating System Gate"]
    types: [completed]

permissions:
  contents: read

jobs:
  clinical-authorization-e2e:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/azaad-clinical-authorization-e2e.yml
    secrets: inherit

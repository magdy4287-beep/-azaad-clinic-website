name: AZAAD Source Canonicality Repair Retirement Marker

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm retired automation
        run: |
          echo "PASS: one-shot source canonicality repair is retired."
          echo "Source canonicalization must be performed through the canonical build/QA path and committed explicitly."

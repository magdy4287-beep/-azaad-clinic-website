name: AZAAD Production Certification Gate

on: workflow_dispatch

permissions:
  contents: read

jobs:
  certification:
    runs-on: ubuntu-latest
    steps:
      - name: Certification workflow is maintained by the canonical Auth Bridge E2E
        run: echo 'Production certification is executed by azaad-auth-bridge-e2e.yml on the exact main SHA.'

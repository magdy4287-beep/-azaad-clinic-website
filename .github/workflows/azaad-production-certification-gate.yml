name: AZAAD Production Certification Gate

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  certification:
    runs-on: ubuntu-latest
    steps:
      - name: Canonical Production Certification Gate
        run: echo 'Production browser certification is owned by azaad-auth-bridge-e2e.yml on the exact commit SHA.'

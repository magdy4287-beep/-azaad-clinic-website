name: Azaad Patient 360 Financial Integration Gate

on:
  push:
    branches: [main, feat/ai-operating-system-20260815]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Patient 360 financial contract
        run: python3 qa/patient-financial-integration-contract.py

This marker intentionally authorizes the controlled database-only Neon migration workflow for this exact commit.

It must be removed after the migration run is verified. Storage migration is deliberately excluded from this workflow.

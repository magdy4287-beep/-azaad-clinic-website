This marker intentionally authorizes the controlled database-only Neon migration workflow for this exact commit.

Execution marker renewed after the workflow boundary was hardened to database-only. Storage migration remains deliberately excluded.

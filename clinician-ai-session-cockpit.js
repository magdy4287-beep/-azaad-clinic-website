(()=>{
'use strict';
if(window.__AZAAD_CLINICIAN_AI_COCKPIT__)return;window.__AZAAD_CLINICIAN_AI_COCKPIT__=true;
const API='/api/clinical-ai-cockpit';const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const en=()=>((document.documentElement.lang||'ar').startsWith('en'));const COPY={ar:{title:'🧠 المساعد الذكي للجلسة',sub:'مساحة سريرية للطبيب — القرار السريري للطبيب دائمًا.',start:'بدء التقييم',finish:'إنهاء الجلسة',save:'حفظ',source:'📚 المصدر',note:'ملاحظة الطبيب/المعالج',yes:'نعم / موجود',no:'لا / غير موجود',skip:'غير مُقيّم',score:'النسبة الحالية',trend:'الاتجاه',baseline:'خط الأساس',previous:'الزيارة السابقة',suggest:'🤖 اقتراح موعد',transfer:'🔁 تحويل لطبيب آخر',aiCandidate:'🤖 سؤال مقترح من AI — يحتاج اعتماد الطبيب.',safety:'⚠️ تنبيهات الأمان تحتاج مراجعة الطبيب.',saved:'تم الحفظ',complete:'اكتمل التقييم'},en:{title:'🧠 AI Session Copilot',sub:'A focused clinician workspace — the clinician remains responsible for clinical decisions.',start:'Start assessment',finish:'Finish session',save:'Save',source:'📚 Source',note:'Clinician note',yes:'Yes / Present',no:'No / Absent',skip:'Not assessed',score:'Current score',trend:'Trend',baseline:'Baseline',previous:'Previous visit',suggest:'🤖 Suggest time',transfer:'🔁 Transfer patient',aiCandidate:'🤖 AI candidate — clinician approval is required.',safety:'⚠️ Safety alerts require clinician review.',saved:'Saved',complete:'Assessment complete'}};
const c=()=>en()?COPY.en:COPY.ar;let state={questions:[],answers:{},session:null,template:null,history:[]};const patientId=()=>window.CURRENT_PATIENT_ID||window.patientId||new URLSearchParams(location.search).get('patient_id')||document.body.dataset.patientId||'';const visitId=()=>window.CURRENT_VISIT_ID||new URLSearchParams(location.search).get('visit_id')||document.body.dataset.visitId||'';
async function req(url,opt={}){const r=await fetch(url,{...opt,credentials:'include',cache:'no-store',headers:{Accept:'application/json','Content-Type':'application/json',...(opt.headers||{})}});const b=await r.json().catch(()=>({}));if(!r.ok)throw Error(b?.error||`HTTP ${r.status}`);return b}
const score=()=>{const a=Object.values(state.answers).filter(v=>v==='yes'||v==='no');return a.length?Math.round(a.filter(v=>v==='yes').length/a.length*100):null};const trend=s=>{if(s==null)return 'insufficient';const p=state.history[0]?.score_percent;if(p==null)return s>=70?'improving':s>=45?'stable':'worsening';return s-p>=10?'improving':s-p<=-10?'worsening':'stable'};
async function start(){if(state.session)return;const r=await req(API,{method:'POST',body:JSON.stringify({action:'session',patient_id:patientId(),clinical_visit_id:visitId()||null,template_id:state.template.id,total_questions:state.questions.length,baseline_score_percent:state.history.at(-1)?.score_percent??null,previous_score_percent:state.history[0]?.score_percent??null})});state.session=r.session;renderQuestions()}
async function answer(q,v){await start();state.answers[q]=v;await req(API,{method:'POST',body:JSON.stringify({action:'answer',session_id:state.session.id,question_id:q,response_boolean:v==='yes'?true:v==='no'?false:null})});await update()}
async function update(done=false){const s=score(),a=Object.values(state.answers).filter(v=>v==='yes'||v==='no').length;await req(API,{method:'POST',body:JSON.stringify({action:'update_session',session_id:state.session.id,answered_questions:a,score_percent:s,baseline_score_percent:state.history.at(-1)?.score_percent??null,previous_score_percent:state.history[0]?.score_percent??null,trend_state:trend(s),follow_up_recommended:false,safety_review_required:false,clinician_notes:$('aiNote')?.value||null,completed_at:done?new Date().toISOString():null})});metrics()}
async function approve(id,ok){await req(API,{method:'POST',body:JSON.stringify({action:'approve_candidate',question_id:id,approved:ok}));const q=state.questions.find(x=>x.id===id);if(q)q.approved=ok;renderQuestions()}
async function favorite(id){await req(API,{method:'POST',body:JSON.stringify({action:'favorite',question_id:id}));renderQuestions()}
function metrics(){const s=score();$('aiScore')&&($('aiScore').textContent=s==null?'—':`${s}%`);$('aiTrend')&&($('aiTrend').textContent=trend(s));$('aiBaseline')&&($('aiBaseline').textContent=state.history.at(-1)?.score_percent??'—');$('aiPrevious')&&($('aiPrevious').textContent=state.history[0]?.score_percent??'—')}
function renderQuestions(){const w=$('aiQuestions');if(!w)return;w.innerHTML=state.questions.map(q=>{const text=en()?(q.question_text||q.question_text_ar):(q.question_text_ar||q.question_text);return `<article class="card" style="margin:10px 0"><div><small>${esc(q.category||'Clinical')} · ${esc(q.source_title||'AZAAD Evidence Library')}</small><h3>${esc(text)}</h3>${q.is_ai_candidate?`<div class="muted">${c().aiCandidate}</div>`:''}</div><div style="display:flex;gap:8px;flex-wrap:wrap"><button class="btn btn-secondary" data-a="${q.id}" data-v="yes">${c().yes}</button><button class="btn btn-secondary" data-a="${q.id}" data-v="no">${c().no}</button><button class="btn btn-secondary" data-a="${q.id}" data-v="skip">${c().skip}</button><button class="btn btn-secondary" data-f="${q.id}">⭐</button>${q.is_ai_candidate&&!q.approved?`<button class="btn btn-primary" data-approve="${q.id}">Approve</button>`:''}</div></article>`}).join('')||'<div class="muted">No approved questions.</div>';w.querySelectorAll('[data-a]').forEach(b=>b.onclick=()=>answer(b.dataset.a,b.dataset.v));w.querySelectorAll('[data-f]').forEach(b=>b.onclick=()=>favorite(b.dataset.f));w.querySelectorAll('[data-approve]').forEach(b=>b.onclick=()=>approve(b.dataset.approve,true))}
async function render(){const host=document.querySelector('#clinicalAssessmentApp')||document.querySelector('main');if(!host||$('azaadClinicianAICockpit'))return;const [t,h]=await Promise.all([req(`${API}?resource=template`),patientId()?req(`${API}?resource=history&patient_id=${encodeURIComponent(patientId())}`):Promise.resolve({history:[]})]);state.template=t.template;state.questions=t.questions||[];state.history=h.history||[];const box=document.createElement('section');box.id='azaadClinicianAICockpit';box.className='card';box.innerHTML=`<div class="panel-head"><div><h2>${c().title}</h2><div class="muted">${c().sub}</div><b>${esc(en()?state.template.name:state.template.name_ar||state.template.name)}</b></div><button id="aiStart" class="btn btn-primary">${c().start}</button></div><div style="display:grid;grid-template-columns:repeat(4,1fr);gap:10px"><div class="card"><b>${c().score}</b><div id="aiScore">—</div></div><div class="card"><b>${c().trend}</b><div id="aiTrend">—</div></div><div class="card"><b>${c().baseline}</b><div id="aiBaseline">—</div></div><div class="card"><b>${c().previous}</b><div id="aiPrevious">—</div></div></div><div id="aiQuestions"></div><label>${c().note}<textarea id="aiNote" rows="3"></textarea></label><div style="display:flex;gap:8px;margin-top:10px"><button id="aiSave" class="btn btn-secondary">${c().save}</button><button id="aiSuggest" class="btn btn-secondary">${c().suggest}</button><button id="aiTransfer" class="btn btn-secondary">${c().transfer}</button><button id="aiFinish" class="btn btn-primary">${c().finish}</button></div><div class="patient-warning">${c().safety}</div>`;host.prepend(box);$('aiStart').onclick=start;$('aiSave').onclick=()=>state.session&&update();$('aiFinish').onclick=()=>state.session&&update(true);$('aiSuggest').onclick=()=>document.querySelector('#doctorFollowupWidget')?.scrollIntoView({behavior:'smooth'});$('aiTransfer').onclick=()=>document.querySelector('#clinicianTransferWidget')?.scrollIntoView({behavior:'smooth'});renderQuestions();metrics()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(()=>render().catch(console.error),700),{once:true});else setTimeout(()=>render().catch(console.error),700);
})();
